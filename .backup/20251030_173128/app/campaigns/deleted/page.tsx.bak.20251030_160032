import React, { Suspense } from 'react';
import dynamic from 'next/dynamic';
import CampaignsDeletedPage from '@/app/_pages/CampaignsDeletedPage';

const SearchFiltersBar = dynamic(() => import('@/components/SearchFiltersBar'), { ssr: false });

export default function Page() {
  const brandOptions: string[] = [];
  const advertiserOptions: string[] = [];

  return (
    <>
      <Suspense>
        <SearchFiltersBar area="archive" brandOptions={brandOptions} advertiserOptions={advertiserOptions} />
      </Suspense>
      <CampaignsDeletedPage />
    </>
  );
}
