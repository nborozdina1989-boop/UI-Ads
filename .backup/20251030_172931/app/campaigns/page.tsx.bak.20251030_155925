import React, { Suspense } from 'react';
import dynamic from 'next/dynamic';
import CampaignsPage from '@/app/_pages/CampaignsPage';

const SearchFiltersBar = dynamic(() => import('@/components/SearchFiltersBar'), { ssr: false });

export default function Page() {
  // Если есть возможность, сюда можно подтянуть списки брендов/рекламодателей (SSR)
  const brandOptions: string[] = [];
  const advertiserOptions: string[] = [];

  return (
    <>
      <Suspense>
        <SearchFiltersBar area="campaigns" brandOptions={brandOptions} advertiserOptions={advertiserOptions} />
      </Suspense>
      <CampaignsPage />
    </>
  );
}
